# PRD:Galaxy 灵感搜索(Galaxy Search)

**Status**: Draft ・ **Author**: Alex(PM)・ **Version**: 0.2
**Stakeholders**: 待定(Engineering / Design / Marketing)
**Last Updated**: 2026(随迭代更新)

> **v0.2 范围修订(2026)**:MVP 由全库 11 类收敛为 **6 类高频交互组件**(Buttons / Checkboxes / Toggle-switches / loaders / Inputs / Radio-buttons,共 2,708 个,占全库 71%),先做深做透,验证"想法 → 匹配"价值后再横向扩展。数据规模基于 `uiverse-io/galaxy` master 实测。

---

## 1. Problem Statement

**问题**:拥有 3,000+ 社区 UI 组件的 galaxy 库,对非开发者几乎不可用。它只有按文件名的代码存档(如 `rare-rattlesnake-22.html`),没有预览图、没有可理解的名称、没有搜索。想找"一个渐变颜色的按钮"的人,面对的是分类目录里的数百个无名 HTML 文件。

**产品范围(v0.2)**:聚焦用户最常描述的 6 类交互组件——按钮、输入框、开关、单选、复选、加载动画。这批组件数量大、需求高频、风格差异明显(渐变/霓虹/3D/拟物/玻璃拟态……),是最适合验证"一句想法 → 视觉匹配"的场景。

**谁在受这个问题困扰,多频繁,代价是什么**:

| 用户 | 场景 | 当前代价 |
|---|---|---|
| 非开发者的设计灵感寻求者(设计师/产品/市场/学生/创业者) | 想知道某类 UI"可以长什么样" | 看不懂代码 → 直接放弃,或去 Dribbble/小红书搜图,拿不到可直接用的实现 |
| 轻度前端用户 | 想快速找"大概这个感觉"的组件 | 在成百上千个文件中盲翻,平均花 10–20 分钟 |

**Evidence 状态说明**:本 PRD 属 0→1,用户证据将在 Spike 阶段补齐(见 §7 验证计划);当前依据为对 galaxy 仓库结构的实测考察(clone master:6 类共 2,708 个组件)+ 行业同类痛点推理。

---

## 2. Goals & Success Metrics

**North Star(上线后 90 天)**:单次会话中找到并打开 ≥3 个"符合想法"的组件预览的用户占比。

| Goal | Metric | Baseline | Target | Window |
|---|---|---|---|---|
| 描述能搜到东西 | 有结果率(无结果 query 占比) | — | 无结果 < 5% | 30 天 |
| 结果真的相关 | Top-5 相关率(人工评测集) | — | ≥ 75% | GA 前固化 |
| 质量按类分层把关(应对 Buttons 一家独大) | Buttons 类 Top-5 相关率(评测集按数量配比加大采样) | — | ≥ 80% | GA 前固化 |
| | Radio-buttons / Checkboxes 等小类 Top-5 相关率(每类固定条数采样) | — | ≥ 70% | GA 前固化 |
| 视觉是核心价值 | 搜索后点击预览率 | — | ≥ 50% | 30 天 |
| 拿得走 | 结果详情页 → 跳转 Uiverse.io / 复制代码 | — | ≥ 25% | 60 天 |
| 值得再来 | 7 日回头率 | — | ≥ 15% | 60 天 |
| 快 | 首屏搜索 P50 耗时 | — | < 1.0s | 30 天 |

---

## 3. Non-Goals(v1 明确不做)

- **不做登录/账号/收藏同步**——收藏用浏览器本地存储起步
- **不做用户上传组件**——数据单源来自 galaxy(避免内容审核与质量成本)
- **不做可视化定制/改样式编辑器**——用户改样式去 Uiverse.io 或自己项目里改
- **不做移动端 App**——响应式 Web 起步;泛用户场景主要是"坐在电脑前找灵感"
- **不做高精度个性化推荐**——"相似组件"用同类别+向量邻近即可,v1.1 再上真实推荐
- **不追求 100% 渲染覆盖率**——预览渲染目标 ≥85% 即 GA,失败组件降级为"仅代码"展示(见 §6 风险 R1)
- **不做英文版界面**(v1 中文界面;检索本身支持中英文 query)
- **本期不做 Cards、Forms、Notifications、Patterns、Tooltips 的搜索**(共 1,094 个组件)。原因:先验证"想法→匹配"在 6 个高频交互类上的质量与用户价值,再横向扩展。**Revisit condition**:6 类 GA 后检索质量达标且分类轴带来自然扩展诉求时,按类型逐类放开(每放开一类跑一次该类评测集)。

---

## 4. Personas & Stories

**Primary Persona — 林悦,28 岁,产品运营**
非开发者。要做一个活动落地页,想先看看"高级感一点的渐变按钮"有哪些好看的样子再定风格。她不关心代码,关心"有没有长这样的、我要的那种感觉"。

**Secondary Persona — 阿哲,24 岁,前端实习生**
知道 Uiverse.io,但记不住组件名。会直接说"帮我找一个霓虹灯效的开关",拿代码回项目改。

### Story 1(核心主线)
> 作为林悦,我想输入一句我对界面元素的描述,就能看到几个"长成我描述那样"的组件预览,以便快速获得设计灵感。

**Acceptance Criteria**:
- [ ] Given 我输入"渐变颜色的按钮",when 提交搜索,then ≤1s 内返回结果网格,每张卡片是可点击的视觉预览图
- [ ] Given 描述含"按钮"(组件类型词),when 返回结果,then 结果以 Buttons 类型为主(硬过滤命中时不出无关类型)
- [ ] Given 描述不含任何类型词(如"炫酷发光"),when 返回结果,then 走纯语义检索,仍返回相关组件并显示命中原因
- [ ] Given 无相关结果,when 提交,then 不显示空页,提示放宽描述并给出分类浏览入口
- [ ] Performance:P50 < 1.0s,P95 < 2.5s

### Story 2(拿代码)
> 作为阿哲,我想打开某个结果看它的代码,以便直接复制到我的项目里。

**Acceptance Criteria**:
- [ ] Given 我点击某张预览卡片,when 详情打开,then 展示大图预览、组件类型/风格标签、原始作者与 Uiverse.io 来源链接
- [ ] Given 该组件为纯 HTML/CSS,when 我点"查看代码",then 可一键复制完整代码
- [ ] Given 该组件渲染失败(无预览图),when 我浏览结果,then 卡片仍可见并明确标注"预览不可用",不阻塞复制代码

### Story 3(分类浏览)
> 作为林悦,我想按"组件类型 × 风格"筛选浏览,以便不靠描述也能逛到灵感。

**Acceptance Criteria**:
- [ ] Given 首页分类浏览,when 我选择类型=Buttons、风格=Gradient,then 只显示同时满足两条件的组件
- [ ] Given 类型筛选轴仅含 6 类(按钮/加载/开关/输入/复选/单选),when 我打开筛选器,then 不出现 Cards 等空分类;页面提供"更多类型即将上线"提示位
- [ ] Given 风格轴来自代码自动探测,when 我切换筛选,then 每个标签旁显示组件数量
- [ ] Given 我未选任何筛选,when 浏览,then 按类型分区展示(与 galaxy 目录一致)

---

## 5. Solution Overview

**产品形态**:单页 Web 应用,首页同时提供两条路径——居中的自然语言搜索框 + 左侧"类型 × 风格"分类筛选器;结果与浏览均为**视觉预览网格**(缩略图卡片),点击进入详情(大图 + 标签 + 作者/来源 + 查看/复制代码)。

**端到端流程**:
1. 用户输入一句话(或点筛选)→ 查询理解:检测是否含**组件类型词**(按钮/加载/开关/输入/复选/单选 → 映射到 galaxy 目录),以及**风格/视觉词**;
2. 检索:类型词命中则先在对应目录内取候选(硬过滤,防"要按钮却出海报");再用**语义向量排序**在候选中找最接近描述者;无类型词则全库(6 类)语义检索;
3. 结果以预览图卡片呈现 → 点击看详情 → 复制代码或跳转 Uiverse.io。

**核心机制——数据管道(产品成立的前提)**:galaxy 提供的是代码,而本产品要卖的是"预览"。因此建库时对每个组件做三件事:① 抽取元数据(组件类型=所在目录;风格特征=从 HTML/CSS 代码启发式探测,如 `gradient`/`neumorphism`/`glassmorphism`/`neon`/`3D`/`animated`/`minimal`/`dark`,解析颜色、圆角、动画等);② **用 headless 浏览器把组件渲染成缩略图**;③ 把"代码特征+标签+类名"拼成描述文档并向量化,建立索引。搜索质量的上限在这里,不在算法。

**类型词 → 目录映射(v0.2,Spike 内扩充同义词)**:

| 用户说法(词表起点) | 映射目录 | 组件数 |
|---|---|---|
| 按钮 / 按键 / button / btn | Buttons | 1,231 |
| 加载 / 转圈 / spinner / 进度动画 | loaders | 718 |
| 开关 / 滑块开关 / toggle / switch | Toggle-switches | 260 |
| 输入框 / 输入 / input / 文本框 | Inputs | 226 |
| 复选框 / checkbox / 勾选 | Checkboxes | 171 |
| 单选 / 单选按钮 / radio | Radio-buttons | 102 |

### Key Design Decisions

| # | 决策 | 理由 | Trade-off |
|---|---|---|---|
| D1 | **预览渲染是 MVP 必做** | 目标用户是泛用户,看不懂代码,"看见"才是价值 | 增加一次性工程成本(批量渲染+失败处理),但无它则产品只是"对开发者友好的 grep" |
| D2 | **类型硬过滤 + 语义排序的混合检索** | 中文口语描述与"组件类型"是两回事;类型词必须精确,风格与感觉交给语义 | 依赖一份中文类型词表(规模小、易维护),未命中词走纯语义兜底 |
| D3 | **embedding 用中文友好模型,索引本地化**(`bge-m3` 本地或 `text-embedding-3-small`),向量检索用 faiss/numpy,**不引向量数据库服务** | 数据仅 2,708 条,量级小;少一个基础设施依赖、成本趋近 0 | v1.1 数据过万或加个性化时需迁移 |
| D4 | **数据源=git 同步 galaxy 仓库**,不爬官网 | 官方仓库即权威镜像,MIT,可自动 `git pull` 增量更新 | 仓库 slug 无语义 → 必须靠数据管道自建语义,且官网 7,000+ 与仓库 3,800+ 有差距,先按仓库做,架构留扩展 |
| D5 | **按风格轴自动探测标签**,不人工标注 | 人工标注不可持续;代码特征(渐变/圆角/动画/配色)可正则可靠抽取 | 抽象风格(如"高级感")探测不到 → 交由语义检索承担 |
| D6 | **结果详情强制展示作者与 Uiverse.io 来源链接** | MIT 允许免署名但鼓励;对社区友好、也符合"灵感库"调性 | 无(纯收益) |

---

## 6. Technical Considerations

### Dependencies

| 依赖 | 用途 | Owner | 时间风险 |
|---|---|---|---|
| uiverse-io/galaxy 仓库(git) | 唯一数据源(6 类 2,708 组件,~12MB) | Eng | Low(公开只读) |
| Chromium/Playwright(headless) | 批量渲染组件 → 预览图 | Eng | Med(见 R1) |
| embedding 模型(bge-m3 本地 或 OpenAI API) | 中文语义向量化 | Eng | Low |
| faiss / numpy | 本地向量检索(2,708 条,单机绰绰有余) | Eng | Low |
| 前端框架(建议 Next.js/React) | Web 应用 | Eng | Low |

### Known Risks

| 风险 | 概率 | 影响 | Mitigation |
|---|---|---|---|
| R1 **渲染失败率高**:组件依赖外部 CDN/Tailwind/字体/图片,或需 JS 交互(hover/点击)才显示,截图可能空白或走样 | Med-High | High(预览是核心价值) | Spike 直接**全量渲染 2,708 个**实测失败率(规模小,无需抽样);**GA gate = 覆盖率 ≥85%**;失败项降级"仅代码"卡片;对纯 hover 才展示的组件可考虑脚本注入状态再截图 |
| R2 **类型规模失衡损害小类召回**:Buttons(1,231)占 6 类 45%,若检索只按全库优化,Radio-buttons(102)/Checkboxes(171) 的召回易被淹没 | High | High | §2 分层质量门槛;评测集按类配比(大类加采样、小类固定条数);检索排序按类归一化 |
| R3 **口语风格词与 CSS 特征鸿沟**(如"赛博朋克""高级感") | Med | Med | 语义检索是主兜底;评测集覆盖此类 query,不达标则 v1.1 引入 LLM 查询改写或 cross-encoder 重排 |
| R4 仓库长期停更/落后官网 | Low-Med | Med | 同步脚本记录 last-sync;架构上数据层与检索层解耦,未来可切官网元数据源 |
| R5 2,708 张截图存储与静态托管成本 | Low | Low | 缩略图压缩为 webp,静态托管;预估 <1GB |

### Open Questions(dev 开始前必须解决)

- [ ] **O1** 6 类类型词表与同义词扩充(起点见 §5 映射表)— Owner: PM — Deadline: Spike 结束
- [ ] **O2** 是否可在 Uiverse.io 官网合法抓取组件标题/作者/标签增强描述文档?— Owner: Eng — Deadline: Spike 结束
- [ ] **O3** 渲染策略:全量一次渲染(2,708,分钟级)vs 按访问热度懒渲染 → v1 取全量 — Owner: Eng
- [ ] **O4** embedding 模型选型:本地 `bge-m3`(免费离线、中文好)vs OpenAI(更省事)— Owner: Eng,倾向本地

---

## 7. Launch Plan

| Phase | 周期 | 范围 | Success Gate |
|---|---|---|---|
| **Spike(可行性验证)** | 第 1 周 | 数据抽取脚本**全量 2,708 个跑通**;度量渲染覆盖率;按类配比跑评测集看 Top-5 相关率;产出 O1–O4 结论 | 渲染覆盖率 ≥85% 且各类评测相关率 ≥60%(Buttons ≥70%),否则回炉调整方案再立项 |
| **MVP Build** | 第 2–5 周 | 数据管道全量(2,708)+ 检索服务 + 前端(搜索/筛选/详情)+ 预览渲染 | 内部可搜索,评测集达标(§2:Buttons ≥80%,小类 ≥70%) |
| **Alpha** | 第 6 周 | 内部 + ≤10 名设计/产品朋友 | 无 P0 bug;用户能在 5 分钟内完成"描述→拿到结果" |
| **Beta** | 第 7–8 周 | 种子用户 30–50 人 | 无结果率 <10%;搜索 P50 <1s;CSAT ≥4/5 |
| **GA** | 第 9 周起 | 公开 | 指标达 §2 目标;预览覆盖率 ≥85% |

**Rollback Criteria**:检索服务不可用时,前端自动降级为纯分类浏览(类型×风格筛选不依赖 embedding);若渲染管线故障,只影响新组件入库,不影响既有索引。

---

## 8. Appendix

### A. 数据源实测结构(v0.2,clone 自 uiverse-io/galaxy master)

| 类型(galaxy 目录) | 组件数 | 是否纳入 |
|---|---:|---|
| Buttons | 1,231 | ✅ |
| loaders | 718 | ✅ |
| Toggle-switches | 260 | ✅ |
| Inputs | 226 | ✅ |
| Checkboxes | 171 | ✅ |
| Radio-buttons | 102 | ✅ |
| **6 类合计** | **2,708(占全库 71%)** | ✅ |
| Cards | 726 | ❌ 本期不做 |
| Forms | 180 | ❌ |
| Patterns | 103 | ❌ |
| Tooltips | 62 | ❌ |
| Notifications | 23 | ❌ |
| **全库合计** | **3,802** | |

> 6 类源码合计约 12MB;全库 3,802 个 HTML 文件。平均单文件仅数 KB,embedding 与索引成本可忽略。

### B. 评测集样例(spike 用,目标 30–50 条,按类配比)

| Query | 期望 | 类型 |
|---|---|---|
| 渐变颜色的按钮 | Buttons ∩ gradient | 类型+风格 |
| 霓虹灯效的开关 | Toggle-switches ∩ neon | 类型+风格 |
| 转圈圈的加载动画 | loaders | 类型 |
| 圆角输入框 | Inputs(语义) | 语义 |
| 红色选中态的复选框 | Checkboxes | 类型+颜色 |
| 胶囊形状的单选 | Radio-buttons(语义) | 语义 |
| 炫酷发光 | 跨类 neon/glow(语义) | 纯语义(无类型词) |
| 赛博朋克感觉的按钮 | Buttons(语义,验收 R3) | 抽象风格 |

### C. 风格探测特征草案

| 风格标签 | 代码特征(正则起点) |
|---|---|
| gradient | `linear-gradient` / `radial-gradient` / `conic-gradient` |
| neon / glow | 彩色 `box-shadow` / `text-shadow` |
| animated | `@keyframes` / `transition` |
| glassmorphism | `backdrop-filter` |
| neumorphism | 同色系双阴影(box-shadow 亮+暗) |
| 3D / minimal / dark 等 | 颜色数、圆角、配色统计(Spike 内定稿) |

### D. 合规

全部组件 MIT;产品界面展示"作者 × Uiverse.io"署名与来源链接。

---

## Changelog

- **v0.2(当前)**:范围收敛为 6 类(2,708 个);新增分层质量指标(§2);Non-Goals 增补 5 类不做及 revisit 条件(§3);类型词映射表定稿 6 类(§5);Spike 改全量渲染(§7);附录更新实测数据表/评测集。连带 UI 决策:分类轴仅显示 6 类 + "更多类型即将上线"提示位。
- **v0.1**:初稿,全库 11 类范围。
